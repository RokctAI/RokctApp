import 'package:flutter/material.dart';
import '{{HOME_IMPORT}}';

class HomeBridge extends StatelessWidget {
  const HomeBridge({super.key});

  @override
  Widget build(BuildContext context) {
    return const {{HOME_WIDGET}}();
  }
}
