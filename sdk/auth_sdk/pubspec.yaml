name: auth_sdk
description: "Authentication and session management SDK for RokctApp."
version: 1.0.0
homepage:

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.5'

dependencies:
  flutter:
    sdk: flutter
  core_sdk:
    git:
      url: https://github.com/RokctAI/flutter_sdk.git
      path: sdk/core_sdk
      ref: ^1.0.0
  firebase_auth: ^6.1.3
  firebase_messaging: ^16.1.0
  google_sign_in: ^6.2.3
  sign_in_with_apple: ^7.0.1
  flutter_facebook_auth: ^7.1.2
  app_tracking_transparency: ^2.0.6+1
  permission_handler: ^12.0.1
  dartz: ^0.10.1
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: 2.10.4
  freezed: 3.2.3
  json_serializable: 6.11.2

flutter:
