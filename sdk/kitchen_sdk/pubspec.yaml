name: kitchen_sdk
description: "Kitchen Display System (KDS) domain models, repository, and workflow providers."
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"
dependencies:
  flutter:
    sdk: flutter
  orders_sdk:
    path: ../orders_sdk
  core_sdk:
    path: ../core_sdk
  flutter_riverpod: ^2.6.1
  freezed_annotation: ^3.1.0

dev_dependencies:
  build_runner: ^2.10.4
  freezed: ^3.2.3

