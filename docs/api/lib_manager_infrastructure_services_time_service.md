# API Reference: time_service

Source file: `lib/manager/infrastructure/services/time_service.dart`

## Classes

### class `TimeService`

## Whitelisted API Endpoints

### `dateFormatYMD(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("yyyy-MM-dd").format(time ?? DateTime.now()); } static String dateFormatMDYHm(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("MMM d, yyyy - HH:mm").format(time ?? DateTime.now()); } static String dateFormatYMDHm(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("yyyy-MM-dd HH:mm").format(time ?? DateTime.now()); } static String dateFormatMDHm(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("MMM d, HH:mm").format(time ?? DateTime.now()); } static String dateFormatDMY(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("d MMM, yyyy").format(time ?? DateTime.now()); } static String dateFormatDM(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("d MMMM").format(time ?? DateTime.now()); } return DateFormat("d MMMM, yyyy").format(time ?? DateTime.now()); } static String dateFormatHM(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("HH:mm").format(time ?? DateTime.now()); } static String dateFormatHME(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("hh:mm, EE").format(time ?? DateTime.now()); } static String dateFormatWDM(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("EEE, d MMM").format(time ?? DateTime.now()); } static String dateFormatForChat(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("EEEE").format(time ?? DateTime.now()); } if ((DateTime.now().difference(time ?? DateTime.now()).inDays) > 7)`
*No documentation provided (generation failed).*

### `DateFormat("MMM/d/yyyy").format(time ?? DateTime.now()); } return DateFormat("HH:mm").format(time ?? DateTime.now()); } static String dateFormatForNotification(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("d MMM, h:mm a").format(time ?? DateTime.now()); } static String formatHHMMSS(int seconds)`
*No documentation provided (generation failed).*

### `timeFormat(DateTime? time)`
*No documentation provided (generation failed).*
