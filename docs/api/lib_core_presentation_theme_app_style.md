# API Reference: app_style

Source file: `lib/core/presentation/theme/app_style.dart`

## Classes

### class `AppStyle`

## Whitelisted API Endpoints

### `SettingsData(), ); if (setting.value == null) return defaultColor; try { return Color( int.parse('0xFF${setting.value!.replaceAll('#', '').substring(0, 6)}'), ); } catch (e)`
*No documentation provided (generation failed).*
