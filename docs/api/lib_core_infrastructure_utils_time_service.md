# API Reference: time_service

Source file: `lib/core/infrastructure/utils/time_service.dart`

## Classes

### class `TimeService`

## Whitelisted API Endpoints

### `dateFormatYMD(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("yyyy-MM-dd").format(time ?? DateTime.now()); } static String dateFormatMDYHm(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("MMM d, yyyy - $_timeFormat", ).format(time ?? DateTime.now()); } static String dateFormatDMY(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("d MMM, yyyy").format(time ?? DateTime.now()); } static String dateFormatMD(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("MMMM dd").format(time ?? DateTime.now()); } static String dateFormatEMD(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("EEEE, MMM dd").format(time ?? DateTime.now()); } static String dateFormatMDHm(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("MMM dd, $_timeFormat").format(time ?? DateTime.now()); } static String dateFormatEE(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("EEEE").format(time ?? DateTime.now()); } static String dateFormatDM(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("d MMMM").format(time ?? DateTime.now()); } return DateFormat("d MMMM, yyyy").format(time ?? DateTime.now()); } static String timeFormat(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat(_timeFormat).format(time ?? DateTime.now()); } static String dateFormatForChat(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("EEEE").format(time ?? DateTime.now()); } if ((DateTime.now().difference(time ?? DateTime.now()).inDays) > 7)`
*No documentation provided (generation failed).*

### `DateFormat("MMM/d/yyyy").format(time ?? DateTime.now()); } return DateFormat(_timeFormat).format(time ?? DateTime.now()); } static String dateFormatForNotification(DateTime? time)`
*No documentation provided (generation failed).*

### `DateFormat("d MMM, $_timeFormat").format(time ?? DateTime.now()); } static String formatHHMMSS(int seconds)`
*No documentation provided (generation failed).*

### `timeFormatTime(String? time)`
*No documentation provided (generation failed).*
